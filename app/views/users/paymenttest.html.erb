<%= asset_path('selectbox.json') %>

<div class="top-hero-wrapper text-center">
  <div class="container top-hero-container">


    <div class="row">
      <%#= form_with url: pay_path do |form| %>
      <%= form_with  url: carddata_path, local:true do |form| %>
      <%#= form_for :user, url: users_path do |f| %>
      <%#= form_for :user, url: users_carddata_path do |form| %>
        <%#= @amount %>
        <%= form.label :"金額(kingaku)", class: 'u_text-white' %>
        <%= form.collection_select( :amount, Kingaku.all, :kingaku, :kingaku, {prompt:  "選択してください"}, {class: "form-control input-sm"}) %>

   


        <%= form.submit "登録", class: 'btn-block btn-white' %>
      <% end %>
    </div>

    <div class="col-md-offset-1 col-md-3">
      <%= link_to 'キャンセル', usergamen_path,class: 'btn btn-block btn-white' %>
    </div>

  </div>
</div>

  